{
  "frontend/src/index.css": "Global styles with green/white OKLCH color palette, Inter/DM Sans fonts, and added max-width:100vw + overflow-x:hidden on html, body, and #root to prevent horizontal page scroll on mobile viewports.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/DM Sans font families, custom `sheet` color tokens (header, even/odd rows), `success` color, card shadows, and fade-in keyframe animation.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations: fetching sheet validity, getting/setting the Google Sheets URL, fetching CSV data, and triggering a manual refresh with cache invalidation.",
  "frontend/src/lib/csvParser.ts": "Utility library that parses a CSV string (handling quoted fields, embedded commas, and newlines) into a structured `{ headers, rows }` object.",
  "frontend/src/pages/SheetViewer.tsx": "Public read-only sheet viewer that hides the 6th column (index 5) from both the table header and all data rows using `visibleHeaders` and `visibleRows` memos; the column count summary and all other display logic reflect only the visible columns.",
  "frontend/src/pages/AdminPanel.tsx": "Admin configuration page at `/admin` with mobile-first responsive layout — URL input and Save button stack vertically on mobile (flex-col sm:flex-row), all card padding uses responsive classes (px-3 sm:px-6), buttons have min-h-[2.5rem] for touch targets, feedback messages use break-words to prevent overflow, and the Back link is always visible on all screen sizes.",
  "frontend/src/App.tsx": "Root app component that sets up TanStack Router with two routes: `/` for SheetViewer and `/admin` for AdminPanel."
}